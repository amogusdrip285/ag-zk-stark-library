{
  "name": "ag-zk-stark-library",
  "version": "1.0.0",
  "description": "A Node.js wrapper for the libstark ZK-STARK library.",
  "main": "index.js",
  "scripts": {
    "install": "node-gyp-build",
    "build": "node-gyp rebuild",
    "prebuild": "prebuildify --napi"
  },
  "dependencies": {
    "node-gyp-build": "^4.8.0"
  },
  "devDependencies": {
    "prebuildify": "^6.0.0",
    "node-addon-api": "^8.5.0",
    "napi-macros": "^2.2.2"
  },
  "gypfile": true
}